/*
|--------------------------------------------------------------------------
| JS CLASICO
|--------------------------------------------------------------------------
*/
function suma (num1, num2) {
    return num1 + num2
}

console.log(suma(4,5))

/*
|--------------------------------------------------------------------------
| ECMAScript
|--------------------------------------------------------------------------
*/

const suma=(num1, num2) =>{
    return num1 + num2
}
console.log(suma(4,5))


//funcion pero con el return implicito
const suma = (num1, num2) => num1 + num2
console.log(suma(4,5))